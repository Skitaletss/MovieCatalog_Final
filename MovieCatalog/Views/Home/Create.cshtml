@model MovieCatalog.Models.Movie
@{
    ViewData["Title"] = "Додати новий фільм";
}

<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: Arial, sans-serif; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 20px; min-height: 100vh; }
        .container { max-width: 800px; margin: 0 auto; }
        .back-btn { display: inline-block; background: white; color: #667eea; padding: 10px 20px; text-decoration: none; border-radius: 8px; font-weight: bold; margin-bottom: 20px; transition: 0.3s; }
        .back-btn:hover { background: #f3f4f6; }
        .form-container { background: white; border-radius: 20px; padding: 40px; box-shadow: 0 20px 60px rgba(0,0,0,0.3); }
        .form-container h1 { color: #667eea; margin-bottom: 30px; text-align: center; font-size: 2em; }
        .form-group { margin-bottom: 25px; }
        .form-group label { display: block; font-weight: bold; color: #374151; margin-bottom: 8px; font-size: 1.1em; }
        .form-group input, .form-group textarea, .form-group select { width: 100%; padding: 12px; border: 2px solid #e5e7eb; border-radius: 8px; font-size: 1em; transition: 0.3s; }
        .form-group input:focus, .form-group textarea:focus { border-color: #667eea; outline: none; }
        .form-group textarea { resize: vertical; min-height: 120px; font-family: Arial, sans-serif; }
        .text-danger { color: #ef4444; font-size: 0.9em; margin-top: 5px; display: block; }
        .form-actions { display: flex; gap: 15px; margin-top: 30px; }
        .btn { flex: 1; padding: 14px; border: none; border-radius: 8px; font-size: 1.1em; font-weight: bold; cursor: pointer; transition: 0.3s; text-align: center; text-decoration: none; display: inline-block; }
        .btn-submit { background: #10b981; color: white; }
        .btn-submit:hover { background: #059669; }
        .btn-cancel { background: #6b7280; color: white; }
        .btn-cancel:hover { background: #4b5563; }
        .file-input-wrapper { position: relative; }
        .file-input-label { display: block; padding: 12px; border: 2px dashed #667eea; border-radius: 8px; text-align: center; cursor: pointer; transition: 0.3s; background: #f9fafb; }
        .file-input-label:hover { background: #f3f4f6; border-color: #764ba2; }
        .file-input { display: none; }
        .info-text { color: #6b7280; font-size: 0.9em; margin-top: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <a href="@Url.Action("Index", "Home")" class="back-btn">⬅️ Назад до списку</a>

        <div class="form-container">
            <h1>➕ Додати новий фільм</h1>

            <form asp-action="Create" method="post" enctype="multipart/form-data">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">
                    <label asp-for="Title">Назва фільму *</label>
                    <input asp-for="Title" class="form-control" placeholder="Введіть назву фільму" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Director">Режисер *</label>
                    <input asp-for="Director" class="form-control" placeholder="Введіть ім'я режисера" />
                    <span asp-validation-for="Director" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Genre">Жанр *</label>
                    <input asp-for="Genre" class="form-control" placeholder="Наприклад: Драма, Бойовик" />
                    <span asp-validation-for="Genre" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Year">Рік випуску *</label>
                    <input asp-for="Year" type="number" class="form-control" placeholder="Наприклад: 2024" />
                    <span asp-validation-for="Year" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Rating">Початковий рейтинг (0-10)</label>
                    <input asp-for="Rating" type="number" step="0.1" min="0" max="10" class="form-control" placeholder="Наприклад: 8.5" value="0" />
                    <span asp-validation-for="Rating" class="text-danger"></span>
                    <span class="info-text">💡 Можна залишити 0, користувачі додадуть оцінки пізніше</span>
                </div>

                <div class="form-group">
                    <label>Постер фільму</label>
                    <div class="file-input-wrapper">
                        <label for="posterFile" class="file-input-label">
                            📁 Виберіть зображення (JPG, PNG)
                        </label>
                        <input type="file" id="posterFile" name="posterFile" accept="image/*" class="file-input" onchange="updateFileName(this)" />
                    </div>
                    <span id="fileName" style="color: #667eea; font-size: 0.9em; margin-top: 5px; display: block;"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Description">Опис *</label>
                    <textarea asp-for="Description" class="form-control" placeholder="Введіть короткий опис фільму"></textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-submit">💾 Зберегти</button>
                    <a href="@Url.Action("Index", "Home")" class="btn btn-cancel">❌ Скасувати</a>
                </div>
            </form>
        </div>
    </div>

    <script>
        function updateFileName(input) {
            const fileName = document.getElementById('fileName');
            if (input.files && input.files[0]) {
                fileName.textContent = '✅ Вибрано: ' + input.files[0].name;
            } else {
                fileName.textContent = '';
            }
        }
    </script>
</body>
</html>
